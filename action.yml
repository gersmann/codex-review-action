name: "Codex Code Review"
description: "Run Codex to review a pull request and post review comments"
author: "OpenAI Codex"
branding:
  icon: "search"
  color: "blue"

inputs:
  mode:
    description: "Operation mode: review"
    required: false
    default: "review"
  openai_api_key:
    description: "OpenAI API key for the selected model provider"
    required: false
  model:
    description: "Review model: gpt-5.6-luna | gpt-5.6-terra | gpt-5.6-sol"
    required: false
    default: "gpt-5.6-luna"
  reasoning_effort:
    description: "Reasoning effort: none | minimal | low | medium | high | xhigh | max"
    required: false
    default: "high"
  web_search_mode:
    description: "Web search mode: disabled | cached | live"
    required: false
    default: "live"
  debug_level:
    description: "Debug verbosity: 0 (off), 1 (basic), 2 (trace)"
    required: false
    default: "1"
  dry_run:
    description: "If '1', print payloads but do not post comments"
    required: false
    default: "0"
  stream_agent_messages:
    description: "If '1', stream model output to logs"
    required: false
    default: "1"
  allowed_commenter_associations:
    description: "Comma-separated GitHub comment author_association values allowed to trigger reviews"
    required: false
    default: "MEMBER,OWNER,COLLABORATOR"
  additional_prompt:
    description: "Extra reviewer instructions appended to the review prompt (verbatim)"
    required: false
    default: ""
  extra_pip_args:
    description: "Additional pip arguments (e.g., --index-url)"
    required: false
    default: ""

runs:
  using: "composite"
  steps:
    - name: Validate inputs
      shell: bash
      env:
        CODEX_REASONING_EFFORT_INPUT: ${{ inputs.reasoning_effort }}
      run: |
        set -euo pipefail
        valid_search_modes="disabled cached live"
        valid_models="gpt-5.6-luna gpt-5.6-terra gpt-5.6-sol"
        contains_word() { case " $1 " in *" $2 "*) return 0;; *) return 1;; esac; }
        # Mode
        case "${{ inputs.mode }}" in review) :;; *) echo "::error::Invalid mode: ${{ inputs.mode }} (allowed: review)"; exit 2;; esac
        # Model
        if ! contains_word "$valid_models" "${{ inputs.model }}"; then
          echo "::error::Invalid model: ${{ inputs.model }} (allowed: $valid_models)"; exit 2;
        fi
        # Reasoning efforts
        PYTHONPATH="${{ github.action_path }}:${PYTHONPATH:-}" python3 -m cli.core.reasoning_effort
        # Web search mode
        if ! contains_word "$valid_search_modes" "${{ inputs.web_search_mode }}"; then
          echo "::error::Invalid web_search_mode: ${{ inputs.web_search_mode }} (allowed: disabled|cached|live)"; exit 2;
        fi
    - name: Prepare review resume state
      if: ${{ inputs.mode == 'review' }}
      id: review_resume_state
      shell: bash
      env:
        GITHUB_TOKEN: ${{ github.token }}
        GITHUB_API_URL: ${{ github.api_url }}
        GITHUB_EVENT_PATH: ${{ github.event_path }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        RUNNER_TEMP: ${{ runner.temp }}
        CODEX_MODEL_INPUT: ${{ inputs.model }}
        GITHUB_ACTION_PATH: ${{ github.action_path }}
      run: |
        set -euo pipefail
        PYTHONPATH="${GITHUB_ACTION_PATH}:${PYTHONPATH:-}" \
          python3 -m cli.review.prepare_resume_state
    - name: Restore review Codex cache
      if: ${{ github.event_name == 'pull_request' && inputs.mode == 'review' && steps.review_resume_state.outputs.restore_key != '' }}
      id: review_codex_cache
      uses: actions/cache/restore@v5
      with:
        path: ${{ steps.review_resume_state.outputs.codex_home }}
        key: ${{ steps.review_resume_state.outputs.restore_key }}
    - name: Install Python dependencies
      shell: bash
      run: |
        set -euo pipefail
        python3 -m pip install --upgrade pip
        python3 -m pip install ${{ inputs.extra_pip_args }} -r "${{ github.action_path }}/requirements.txt"

    - name: Run Codex review (CLI)
      id: run_codex_cli
      shell: bash
      env:
        # Tokens
        GITHUB_TOKEN: ${{ github.token }}
        OPENAI_API_KEY: ${{ inputs.openai_api_key }}
        # Config
        CODEX_MODE: ${{ inputs.mode }}
        CODEX_MODEL: ${{ inputs.model }}
        CODEX_REASONING_EFFORT: ${{ inputs.reasoning_effort }}
        CODEX_WEB_SEARCH_MODE: ${{ inputs.web_search_mode }}
        CODEX_ALLOWED_COMMENTER_ASSOCIATIONS: ${{ inputs.allowed_commenter_associations }}
        CODEX_ADDITIONAL_PROMPT: ${{ inputs.additional_prompt }}
        DEBUG_CODEREVIEW: ${{ inputs.debug_level }}
        DRY_RUN: ${{ inputs.dry_run }}
        STREAM_AGENT_MESSAGES: ${{ inputs.stream_agent_messages }}
        CODEX_HOME: ${{ steps.review_resume_state.outputs.codex_home }}
        CODEX_REVIEW_PREVIOUS_HEAD_SHA: ${{ steps.review_resume_state.outputs.previous_reviewed_sha }}
        CODEX_REVIEW_CACHE_HIT: ${{ steps.review_codex_cache.outputs.cache-hit }}
      run: |
        set -euo pipefail
        # Execute the CLI directly; it detects GitHub Actions via env
        PYTHONPATH="${{ github.action_path }}:${PYTHONPATH:-}" \
          python3 -m cli.main
    - name: Save review Codex cache
      if: ${{ github.event_name == 'pull_request' && inputs.mode == 'review' && steps.run_codex_cli.outcome == 'success' && steps.review_resume_state.outputs.current_cache_key != '' && !(steps.review_codex_cache.outputs.cache-hit == 'true' && steps.review_resume_state.outputs.restore_key == steps.review_resume_state.outputs.current_cache_key) }}
      uses: actions/cache/save@v5
      with:
        path: ${{ steps.review_resume_state.outputs.codex_home }}
        key: ${{ steps.review_resume_state.outputs.current_cache_key }}
